import { ClampTextPipe } from './clamp-text.pipe';

describe('ClampTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ClampTextPipe();
    expect(pipe).toBeTruthy();
  });
});
