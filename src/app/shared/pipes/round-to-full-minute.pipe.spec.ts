import { RoundToFullMinutePipe } from './round-to-full-minute.pipe';

describe('RoundToFullMinutePipe', () => {
  it('create an instance', () => {
    const pipe = new RoundToFullMinutePipe();
    expect(pipe).toBeTruthy();
  });
});
